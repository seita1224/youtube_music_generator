import { ScreenPlaceholder } from "@/components/admin/screen-placeholder";

// タイムライン(ADR-0041): 出来事ログ + 当日サマリ。

export default function TimelinePage(): React.JSX.Element {
  return (
    <ScreenPlaceholder
      title="タイムライン"
      description="出来事ログと当日サマリ。"
    />
  );
}
